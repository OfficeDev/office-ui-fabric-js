<h1 id="link">Link</h1>
<p>Used to create a hyperlink to another location.</p>
<h2 id="variants">Variants</h2>
<h3 id="default">Default</h3>

{{> Link props=LinkModels.basic }}

<h2 id="states">States</h2>
<p>This component has only the default state.</p>
<h2 id="using-this-component">Using this component</h2>
<ol>
    <li>Confirm that you have references to Fabric&#39;s CSS on your page:<pre><code> &lt;head&gt;
     &lt;link rel=&quot;stylesheet&quot; href=&quot;fabric.min.css&quot;&gt;
     &lt;link rel=&quot;stylesheet&quot; href=&quot;fabric.components.min.css&quot;&gt;
 &lt;/head&gt;
</code></pre></li>
    <li>Copy the HTML from one of the samples above into your page. For example:
        <pre>
 <code>
{{renderPartialPre "Link" "LinkExample" LinkModels.basic false}}
 </code>
</pre>
    </li>
    <li>Replace the sample text with the text of your link.</li>
</ol>
<h2 id="dependencies">Dependencies</h2>
<p>This component has no dependencies on other components.</p>
<h2 id="accessibility">Accessibility</h2>
<p>Use the <code>title</code> attribute to provide additional text describing the link, if the link text itself is not descriptive enough.</p>
<h2 id="notes">Notes</h2>
<p>The Link inherits the font family, size, and weight of its parent element. This is why we have placed it within <code>.ms-font-m</code> wrapper in the sample above.</p>